# Salvataggio cloud (opzionale): senza queste due variabili l'app funziona
# esattamente come oggi, solo in locale (nessun login, nessun errore).
# Valori nel dashboard Supabase: Project Settings > API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
